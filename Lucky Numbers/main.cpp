#include <iostream>

using namespace std;

int main()
{
    int A,B;
    cin >> A >> B;
    for(int i=A;i<=B;i++)
    {
        if(i%7 == 0 || i%4 == 0)
            if((i/7)%7 == 0 || )
    }
    return 0;
}
