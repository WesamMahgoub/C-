#include <iostream>
#include <string>
using namespace std;

void pop_back(string ss)
{
    ss[ss.size()-1] = NULL;
}
int main()
{
    int N,q;
    cin >> N >> q;
    string S;
    cin >> S;

    return 0;
}
